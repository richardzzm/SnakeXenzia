module dayi.SnakeXenzia {
    // 游戏中的一个位置
    export class Point {
        x: number;
        y: number;

        constructor(x: number, y: number) {
            this.x = x;
            this.y = y;
        };
    };
}